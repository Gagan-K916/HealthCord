import 'package:flutter/material.dart';

// These are the colors of the logo. Trying to match the whole app with these color constants
const primaryColor =Color(0XFF0A2640);
const secondaryColor = Color(0XFF65E4A3);

const cardColor = Color(0XFF65E4A3);

Color bgColor = Colors.grey.shade300;

Color defaultBorderColor = Colors.grey.shade500;
