const double circularRadius = 20.0;
const double defaultPadding = 8.0;

const double defaultFontSize = 16.0;

const double defaultCardHeight = 140;

const defaultAnimationDuration = Duration(milliseconds: 200);
